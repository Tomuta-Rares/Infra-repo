apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopping-stack.fullname" . }}
  labels:
    {{- include "shopping-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopping-stack.selectorLabels" . | nindent 4 }}
