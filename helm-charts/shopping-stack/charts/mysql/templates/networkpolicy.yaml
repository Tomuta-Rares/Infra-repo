apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-shopping-to-mysql
  namespace: mysql
spec:
  podSelector:
    matchLabels:
      app: mysql

  policyTypes:
    - Ingress

  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: default
          podSelector:
            matchLabels:
              app: shopping
      ports:
        - protocol: TCP
          port: {{ .Values.service.port }}