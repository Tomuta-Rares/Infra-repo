apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    app: shopping-frontend-service
spec:
  type: ClusterIP
  selector:
    app: shopping-frontend
  ports:
    - port: 80
      targetPort: 80